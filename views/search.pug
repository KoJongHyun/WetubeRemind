
extends layouts/main
include mixins/videoBlock

block content
  .container
    h3 Searching by #{searchingBy}
    .videos
      each item in videos
        +videoBlock({
          id: item.id,
          title: item.title,
          views: item.views,
          videoFile: item.videoFile
        })