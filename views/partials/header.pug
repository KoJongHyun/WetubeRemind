header.header
  .container.container--header
    .logo
      a(href=routes.home)
        i.fab.fa-youtube
    .search
      form(action=routes.search, method="GET")
        input(type="text", name="term", placeholder="Search...")
    .nav
      ul.nav__inner
        li.nav__list
          a.nav__link(href=routes.login) Login
        li.nav__list
          a.nav__link(href=routes.logout) Logout